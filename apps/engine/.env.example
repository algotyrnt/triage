# PostgreSQL Connection URL (required)
DATABASE_URL=postgres://triage:triage_secret_password@localhost:5432/triage_db?sslmode=disable

# Engine Port (default: 8080)
PORT=8080

# Structured Logging Level (debug, info, warn, error)
LOG_LEVEL=info
