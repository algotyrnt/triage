# Production Docker Compose for Triage Stack
# Usage:
#   TRIAGE_VERSION=v0.1.0 docker compose -f docker-compose.prod.yml up -d
#
version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: triage-db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-triage}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-triage_secret_password}
      POSTGRES_DB: ${POSTGRES_DB:-triage_db}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-triage} -d ${POSTGRES_DB:-triage_db}']
      interval: 5s
      timeout: 5s
      retries: 5

  engine:
    image: ghcr.io/algotyrnt/triage-engine:${TRIAGE_VERSION:-latest}
    container_name: triage-engine
    restart: always
    ports:
      - '${ENGINE_PORT:-8080}:8080'
    environment:
      PORT: '8080'
      DATABASE_URL: 'postgres://${POSTGRES_USER:-triage}:${POSTGRES_PASSWORD:-triage_secret_password}@postgres:5432/${POSTGRES_DB:-triage_db}?sslmode=disable'
      GEMINI_API_KEY: '${GEMINI_API_KEY:-}'
      APP_URL: '${APP_URL:-http://localhost:3000}'
    depends_on:
      postgres:
        condition: service_healthy

  dashboard:
    image: ghcr.io/algotyrnt/triage-dashboard:${TRIAGE_VERSION:-latest}
    container_name: triage-dashboard
    restart: always
    ports:
      - '${DASHBOARD_PORT:-3000}:3000'
    environment:
      PORT: '3000'
      NEXT_PUBLIC_ENGINE_URL: '${NEXT_PUBLIC_ENGINE_URL:-http://localhost:8080/api/v1/telemetry}'
    depends_on:
      - engine

volumes:
  postgres_data:
